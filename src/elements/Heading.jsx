import styled from 'styled-components';

const Heading = styled.h1`
  margin: 0;
  font-size: 2.2em;
  font-weight: 500;

  line-height: 110%;

  @media print {
    margin-top: 0;
  }
`;

export default Heading;
